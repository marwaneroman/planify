# Backend — staging environment (Docker / compose)
DATABASE_URL="postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-admin}@db:5432/${POSTGRES_DB:-planify}"
POSTGRES_ADMIN_URL="postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-admin}@db:5432/postgres"
JWT_SECRET="staging-secret-change-me"
PORT=3000
CORS_ORIGIN="http://localhost,http://127.0.0.1"

