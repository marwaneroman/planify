# Used by docker-compose (run from project root)
POSTGRES_USER=planify
POSTGRES_PASSWORD=change-me
POSTGRES_DB=planify
JWT_SECRET=change-me-to-a-long-random-string

# Optional: for frontend when running "npm run dev" inside frontend/
# VITE_API_URL=http://localhost:3001
